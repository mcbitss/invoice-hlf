---
#
# The network connection profile provides  the information about the target
# blockchain network that are necessary for the applications to interact with it.
#
name: "invoice-app-org2"
description: "invoice app Network - client definition for Org2"
version: "1.0"

#
# The client section is (node.js) SDK-specific 
#
client:
  organization: Org2
  credentialStore:
    path: "./fabric-client-kv-org2"
    cryptoStore:
      path: "/tmp/fabric-client-kv-org2"
